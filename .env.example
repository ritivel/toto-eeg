# Copy to `.env` and fill in real values. `.env` itself is gitignored.

# Weights & Biases — used by Toto 2.0 training when WandbLogger is enabled
WANDB_API_KEY=
WANDB_ENTITY=
WANDB_PROJECT=toto2-eeg

# HuggingFace — only needed when continuing pre-training from a private repo
HF_TOKEN=
